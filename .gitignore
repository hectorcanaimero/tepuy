# Claude Code — se comparten agents/ y commands/; se ignoran los settings locales
.claude/settings.json
.claude/settings.local.json

# macOS
.DS_Store

# Flutter / Dart (para cuando arranque el proyecto)
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
*.iml
.idea/
.vscode/
# pubspec.lock SE commitea en apps (build reproducible) — no ignorar.
ios/Pods/
ios/.symlinks/
android/.gradle/
android/local.properties
# Código generado (Drift .g.dart, etc.) SE commitea: no hay paso de codegen en CI,
# así clonar y correr funciona sin build_runner. Regenerar: dart run build_runner build

# Secrets / entorno
.env
.env.*

# Tooling local de skills/agents (no es código del proyecto)
.agents/
.claude/skills/
skills-lock.json
